spring:
  application:
    name: TestProject
  profiles:
    active: dev
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/course
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8082